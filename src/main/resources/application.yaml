spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5433/twogether_db
    username: twogether
    password: twogether
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080